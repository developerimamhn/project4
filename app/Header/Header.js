import React from 'react';
import Navbar from './Navbar';
import HeroArea from './HeroArea';

const Header = () => {
    return (
        <div>
            <Navbar/>
            <HeroArea/>
        </div>
    );
};

export default Header;